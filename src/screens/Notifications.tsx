import { View, Text } from "react-native"
import { globalStyle } from "../styles/global"

const Notifications = () => {
    return (
        <View style={globalStyle.screenContainer}>
            <Text style={globalStyle.title}>Notifications</Text>
        </View>
    )
}

export default Notifications